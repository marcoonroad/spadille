'use strict';var _require=require('../utils/environment');const isNode=_require.isNode,isBrowser=_require.isBrowser;let secret=null;(function(){if(isBrowser()){secret=require('./browser').init().secret}else if(isNode()){secret=require('./node').init().secret}else{throw Error('Could not detect execution context!')}})();const generate=secret;module.exports.generate=generate;